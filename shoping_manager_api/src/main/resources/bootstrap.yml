spring:
  profiles:
    # 环境
    active: dev
  cloud:
    nacos:
      # 注册中心
      config:
        server-addr: 10.0.0.41:8848 #注册中心地址
        namespace: 13f67658-3186-46df-b4c3-0adbbf038e01 #命名空间
        file-extension: yaml #文件名后缀
        prefix: shoping_manager_api